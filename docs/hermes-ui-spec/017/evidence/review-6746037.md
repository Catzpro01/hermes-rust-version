# Code review — checkpoint 6746037

Tanggal: 2026-09-14. Permintaan terakhir `/code-review` didahulukan; tidak
mengasumsikan `/implement` yang terinterupsi telah berjalan. Baseline 059cd65
memakai pilihan user sebelumnya (all ANSI). Metode tetap **langsung terbatas**,
bukan dua subagent independen atau review pribadi Matt Pocock.

## Batas pemeriksaan

- Baseline: `059cd6513ee6abeeec0cc61e478dcb38896634d2`.
- Checkpoint dibekukan: `6746037ad53e1bfaab0ef311d8cd55527ceefe2f`.
- Ref resolve; diff tidak kosong, 117 path / 24 commit.
- Perintah diff dicatat sebelum membuat laporan:

```sh
git diff 059cd6513ee6abeeec0cc61e478dcb38896634d2...HEAD
# HEAD saat diambil = 6746037ad53e1bfaab0ef311d8cd55527ceefe2f
git log 059cd6513ee6abeeec0cc61e478dcb38896634d2..6746037ad53e1bfaab0ef311d8cd55527ceefe2f --oneline
```

Reproduksi frozen diff menggunakan SHA checkpoint sebagai pengganti HEAD.
Standards: AGENTS.md, CONTEXT.md, ADR 0001/0002,
`docs/agents/skills/progress-handoff/SKILL.md` dan seluruh 12 smell heuristik
pada `.agents/skills/code-review/SKILL.md`; aturan repo mengungguli heuristik,
format/lint yang ditangani tooling tidak dihitung sebagai temuan review.
Spec: `docs/HERMES_UI_SPEC.md` §A/§J.7, T12 dan kesepakatan Q1–Q8 di
`.scratch/hermes-rs-total-parity/grilling.md`; allowlist adaptasi docs/PARITY.md
pada b38d09e. Scope sumber: delta runtime/tes welcome.rs, workflow/gate dan
capture metadata; bukti generated diperiksa integritas serta dump sel.

## Standards

**0 temuan baru yang terkonfirmasi** dalam pemeriksaan terbatas ini.
Runtime tetap terlokalisasi pada welcome.rs: alokasi kolom, centering teks
terlihat, pelestarian spasi, reset SGR dan styling. Tidak ada perubahan
arsitektur, fitur tambahan, instalasi Python atau perluasan permission CI.

Temuan heuristik duplikasi decoder pada review sebelumnya telah ditangani
melalui `observed_banner_cells`; regresi public writer menggunakannya bersama.
Fixture Python terpisah dari output Rust dan koreksi centering tidak mengganti
expected agar mengikuti Rust. Riwayat RED/GREEN dan penerapan exact tested
delta tercatat di tdd-followup.md. Tidak ditemukan alasan baru untuk refactor
heuristik tambahan. Ini bukan jaminan bebas bug atau approval independen.

## Spec

**P1 — Tinggi untuk closure: cakupan bukti §J.7 masih parsial.**
`docs/HERMES_UI_SPEC.md:837–839` mewajibkan “side-by-side capture ... banner
panel (title + grid), setup wizard tiap step (curses frame), session picker
frame, completion dropdown (string candidates), summary line.” Q3 juga
mewajibkan normal/risky states dan summary nol/non-nol. Pada checkpoint ini,
T12:34–38 masih terbuka: wizard tiap langkah/cancel/unavailable, picker
normal/empty/filter/delete-confirmation, kandidat REPL dan summary nol/non-nol.
Tes dan import probe tidak memenuhi bukti visual tersebut.

Tindakan: lanjutkan `/implement` T12 dengan capture wizard nyata terlebih
dahulu, kemudian area lain; koreksi cacat melalui RED/GREEN, review delta dan
recapture sumber committed. Pertahankan raw/PNG serta metadata per kasus.
P1 adalah kekurangan deliverable yang diakui, bukan regresi runtime baru.

Kebutuhan recapture banner pada review lama sudah terpenuhi untuk fixture
5a8e12c. Tidak ditemukan penyimpangan runtime baru atau scope creep dari diff
ini; branding tetap adaptasi terdokumentasi. Q8 tetap memerlukan bukti lengkap,
CI relevan hijau dan explicit final acceptance sebelum closure. Tidak ada merge.

## Verifikasi yang benar-benar dilakukan pada review ini

- `PYTHONPATH=/home/user/.cache/ci-review-py python3 scripts/test_ci_workflow.py`:
  **8 tes PASS** (lokal).
- SHA-256/ukuran seluruh 99 file terdaftar pada integrity.json dari paket
  a2a3d08, b56c9a3 dan 5a8e12c cocok; tidak ada bukti yang ditulis ulang.
- Paket terbaru: delapan raw/event/cast round trip cocok; hash source cocok
  dengan welcome.rs saat ini, source bundle 5a8e12c dan diff Rust kosong.
- Dump sel terbaru: jumlah baris/sel cocok, glyph non-judul cocok; atribut
  glyph sama serta background/inverse/underline cocok. Tidak menormalisasi
  output. Ini pemeriksaan ulang dump, bukan delapan screenshot baru; inspeksi
  langsung PNG telah dilakukan pada tahap capture sebelumnya.
- `git diff 5a8e12c...6746037 -- crates scripts .github` kosong.
- Query GitHub: **CI 34786290998 SUCCESS**, head tepat 6746037. Ini run yang
  sudah ada dan diverifikasi kembali, bukan run Rust lokal atau rerun baru.
  Capture aktual tetap 34785921221/5a8e12c; run dokumentasi bukan recapture.
- `git diff --check` bersih sebelum penulisan laporan.

## Commit yang dicakup

```text
6746037 Record ask-matt route for remaining T12 UI evidence
b349ee1 Retain inspected centered-banner evidence and reconcile UI progress
5a8e12c Fix visible-text centering and recapture reviewed committed source
97ce7b4 Validate wrapped-label centering after observed one-column RED
5a2207d Retain post-review captures and reproduce residual session centering
b56c9a3 Resolve review duplication and capture the committed banner fixes
c2aca92 Record limited two-axis review and validate test-decoder deduplication
e0a52fd Record green committed-source CI and pending review choices
3e7c895 Fix tool punctuation colors and preserve review-before-capture boundary
7af74af Validate per-token tool colors after observed punctuation RED
98793df Fix secondary banner dim and submit tool-punctuation regression
871faaf Validate banner dim and cropped-session style preservation after RED
b4e0ee7 Fix ANSI style carry-over and submit separate dim regression
3dffb6c Validate SGR reset after observed border-bold regression
9aef403 Fix long-session column allocation and submit separate bold regression
33209fd Validate allocated banner columns after confirmed long-session RED
cc458e9 Submit exact-fixture long-session banner regression for RED
7f7854d Route next Hermes parity slices through installed ask-matt skill
905b5e9 Record real post-fix banner evidence and remaining layout/style defects
a2a3d08 Fix ANSI banner column collapse with runner-verified regression
3f3d996 Validate ANSI space preservation against corrected RED baseline
209d04e Match ANSI regression inputs to each Python reference before repeating RED
1f0db4f Report precise ANSI regression differences without masking candidate failure
e92cc63 Submit minimal ANSI whitespace correction after confirmed RED regression
```

**Ringkasan: Standards 0 temuan baru (tidak ada isu terburuk yang ditemukan);
Spec 1 temuan terbuka, terberat cakupan bukti §J.7 belum lengkap.**
