# Diagnostic packet — NOT acceptance evidence

Source 1e3abe7; CI 34787458843 and transport/case gate 34787458859 succeeded.
The first 24 wide PNGs were directly inspected. That inspection exposed a
capture defect despite the gate: exited summary process output was not fully
drained, yielding blank/truncated Rust summary screenshots. New real-PTY RED
and GREEN correct the recorder. Do not call these images parity failures of
the Rust banner or relabel this packet PASS. The narrow PNGs were not all
inspected; no complete visual review of this diagnostic packet is claimed.

Gateway Python frames also contain missing aiohttp/cryptography plugin
warnings. Dependencies are being installed only into isolated cache. The
upstream gateway empty flow attempted service setup: its temporary-home guard
refused the unit write, and user systemd/linger operations failed. The next
capture explicitly denies external process launches in the Python child;
no service setup is authorized or intentionally performed. Full raw remains.

Picker frames show real differences (header/row/footer placement and colors;
separate documented ID/status/relative-time adaptations). Rust completion
cycles choices inline while the Python native completer host displays a menu.
These observations require a clean packet before final findings/acceptance.
No normalization, deletion, or overwriting of this packet. No closure/merge.
