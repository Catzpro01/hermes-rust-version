# Intermediate packet — caller alignment pending

This is an immutable intermediate recording, not final acceptance evidence.
Actual source 1c3c9dd: CI 34787791435 and capture 34787791444 SUCCESS. All 48
Python/Rust case gates reached; Python plugin dependencies were supplied and
external-process/network activity explicitly blocked. Full raw recordings
include bytes after explicitly selected notice/section snapshot boundaries.

Summary counts have matching absolute row/column positions, but the component
adapter omits the terminal newline that Python Console.print and the actual
Rust REPL caller emit. At 100 columns this yields 32 Python vs31 Rust emulator
rows and shifts the ending viewport. Adapter-only correction is being checked
and recaptured in a new packet, not hidden by image normalization.

PNG files in this intermediate packet have not received a complete direct
image review. Do not inherit the predecessor's image inspection as this
packet's review. Original streams/images must not be changed or relabeled PASS.
